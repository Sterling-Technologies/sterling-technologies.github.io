jQuery(function($) {
    /*$(window).on('fixed-scroll-init', function(e, target) {
        var clone = $(target).clone().addClass('fixed-scroll');
        $(window).scroll(function(e) {
            if($(this).scrollTop() > 100) {
                clone.appendTo(document.body);
            } else {
                clone.remove();
            }
        });
    });*/

    $(document.body).doon();
});
